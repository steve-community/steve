# Steve Docker Compose Configuration
# Copy this file to .env and modify values as needed

# Application Ports (exposed to host)
HTTP_PORT=8180
HTTPS_PORT=8443

# NOTE: Database credentials (user, password, schema) are configured in
# src/main/resources/application-docker.properties and baked into the
# application at Maven build time. Changing them here has no effect on
# the application — only on the MariaDB container.
# To change DB credentials, edit application-docker.properties and rebuild.
