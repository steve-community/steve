# Just to be backwards compatible with previous versions, this is set to "steve",
# since there might be already configured chargepoints expecting the older path.
# Otherwise, might as well be changed to something else or be left empty.
#
context.path = steve

# Database configuration
#
db.ip = localhost
db.port = 3306
db.schema = stevedb
db.user = steve
db.password = StevePass2026!

# Credentials for Web interface access
#
auth.user = admin
auth.password = 1234

# The header key and value for Web API access using API key authorization.
# Both must be set for Web APIs to be enabled. Otherwise, we will block all calls.
#
#webapi.key = STEVE-API-KEY
#webapi.value =  

# ─────────────────────────────────────────────────────
# REST API CONFIGURATION (ADD THESE)
# ─────────────────────────────────────────────────────
rest.enabled=true
rest.api.path=/steve/api/v1

# Jetty configuration
#
#server.host = 0.0.0.0
server.address = 0.0.0.0
server.port = 8080
server.gzip.enabled = true

# Jetty HTTP configuration
#
#http.enabled = true
#http.port = 8080

# Jetty HTTPS configuration
#
https.enabled = false
https.port = 8443
keystore.path =
keystore.password =

# OCPP WebSocket Server (CRITICAL for chargers)
#
ocpp.j.server.port = 8880
ocpp.j.server.host = 0.0.0.0

# When the WebSocket/Json charge point opens more than one WebSocket connection,
# we need a mechanism/strategy to select one of them for outgoing requests.
# For allowed values see de.rwth.idsg.steve.ocpp.ws.custom.WsSessionSelectStrategyEnum.
#
ws.session.select.strategy = ALWAYS_LAST

# if BootNotification messages arrive (SOAP) or WebSocket connection attempts are made (JSON) from unknown charging
# stations, we reject these charging stations, because stations with these chargeBoxIds were NOT inserted into database
# beforehand. by setting this property to true, this behaviour can be modified to automatically insert unknown
# stations into database and accept their requests.
#
# CAUTION: setting this property to true is very dangerous, because we will accept EVERY BootNotification or WebSocket
# connection attempt from ANY sender as long as the sender knows the URL and sends a valid message.
#
auto.register.unknown.stations = false

# if this field is set, it will take precedence over the default regex we are using in
# de.rwth.idsg.steve.web.validation.ChargeBoxIdValidator.REGEX to validate the format of the chargeBoxId values
#
charge-box-id.validation.regex =

### DO NOT MODIFY ###
db.sql.logging = false
profile = prod

# SteVe Webhook Plugin Environment Variables
#voltstartev.webhook.url=http://localhost:3000/api/webhooks/steve
#voltstartev.webhook.secret=your-super-secret-webhook-key-min-32-characters
#voltstartev.webhook.connect-timeout=3000
#voltstartev.webhook.read-timeout=5000
#voltstartev.webhook.retry-max-attempts=3
#voltstartev.webhook.meter-values-sampling-enabled=true
#voltstartev.webhook.meter-values-min-interval-seconds=30
# ─────────────────────────────────────────────────────
#  DEBUG LOGGING (Enable for troubleshooting)
# ─────────────────────────────────────────────────────
logging.level.de.rwth.idsg.steve=DEBUG
logging.level.de.rwth.idsg.steve.ocpp=DEBUG
logging.level.de.rwth.idsg.steve.ocpp.ws=DEBUG
logging.level.de.rwth.idsg.steve.service.reservation=DEBUG
logging.level.de.rwth.idsg.steve.service.transaction=DEBUG
logging.level.de.rwth.idsg.steve.web.api=DEBUG
logging.level.de.rwth.idsg.steve.ws=DEBUG

# VoltStartEV Webhook Integration
#voltstartev.webhook.url=http://localhost:3000/api/webhooks/steve
voltstartev.webhook.url=http://127.0.0.1:3000/api/webhooks/steve
voltstartev.webhook.secret=507f23bc3c256e54390ee5478eb42881a6b4c30d6a260a7f036fe6fe00e43bb7

# VoltStartEV Webhook Integration
#voltstartev.webhook.enabled=true
#voltstartev.webhook.url=https://api.voltstartev.com/api/webhooks/steve
#voltstartev.webhook.secret=${WEBHOOK_SECRET}
# Timeouts
#voltstartev.webhook.connect-timeout-ms=3000
#voltstartev.webhook.read-timeout-ms=5000
# Retry
#voltstartev.webhook.retry.max-attempts=5
#voltstartev.webhook.retry.initial-delay-ms=1000
# Async
#voltstartev.webhook.async=true
# Events to send
#voltstartev.webhook.events=StatusNotification,StartTransaction,StopTransaction,MeterValues
# Security
#voltstartev.webhook.signature-header=X-VoltStart-Signature
