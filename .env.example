# Steve Docker Compose Configuration
# Copy this file to .env and modify values as needed

# Database Configuration
DB_NAME=stevedb
DB_USER=steve
DB_PASSWORD=changeme_secure_password_here
DB_PORT=3306

# Application Ports
HTTP_PORT=8180
HTTPS_PORT=8443

# Java Options
JAVA_OPTS=-XX:MaxRAMPercentage=85 -Djdk.tls.client.protocols=TLSv1,TLSv1.1,TLSv1.2

# Security Note: 
# Please change the default DB_PASSWORD before deploying to production!
# Consider using Docker secrets for sensitive data in production environments.